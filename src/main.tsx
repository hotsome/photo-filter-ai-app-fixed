import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div className="text-3xl font-bold underline text-center mt-10">
      Photo Filter AI 完全版起動！🎉
    </div>
  </React.StrictMode>
)
